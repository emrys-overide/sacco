rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Sacco OS Zero-Trust Security Rules
    // Direct client-side reads and writes are blocked by default.
    // All application data access should flow through the secure backend API.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
