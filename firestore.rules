rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Sacco OS Zero-Trust Security Rules
    // All direct client-side reads and writes are strictly forbidden.
    // Database access is mediated 100% securely through the Express server.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
